as client folder already created (empty)

//PROJECT INTIALISATION WITH CLEANUP
now i have to install react project using vite
1. so i am using command
-> npm create vite@latest . -- --template react
-> npm install (for creating node_module)

so our directory tree will look like that

node_modules
public
src
 ┣ App.jsx
 ┣ index.css
 ┗ main.jsx
.gitignore
eslint.config.js
HowIDoIt-frontend.txt
index.html
package-lock.json
pacakge.json
README.md 
vite.config.js

2. delete 2 files App.css and assets folder (not need) because I will use tailwind css

//PREREQUIREMENTS (install some packages and some empty folders/files)
1.  npm install  axios    (for making http req from frontend to backend)
2.  npm install tailwindcss @tailwindcss/vite    (using tailwindCSS) //i am following tailwindcss documentation for that
    configure vite plugin (update vite.config.js)
    update index.css -> @import "tailwindcss";
3.  create new folders in src
      config
      features/items
      shared/components or hooks

//ACTUAL CODE
1. create config/api.js  -> for directly using api anywhere wihout repeating use of axios
2. create shared/components/Loader.jsx    (shared loader for overall application)
3. create shared/components/ErrorMessage.jsx 
4. create shared/components/Layout.jsx
5. create features/items/api/items.api.js   (for calling backend endpoints)
6. install react-toastify (for showing toasts)
7. update App.jsx (include toast at root level)
8. create items/hooks/useItems.js 

//Deploy
1. Render -> for backend deploy (
    1. connect github repo
    2. add env variables 
    3. path of start change 
      -> node index.js -> node src/server.js)
2. Netlify -> for frontend deploy (connected via github and or update base url (render backend url))
3. Betterstack -> for hit /health api (for always waking up the server)

//NOW BEAUTIFICATION
1. added modal instead of a direct form (new file -> shared/components/Modal.jsx)
2. theme added 
    update index.css for accept dark theme
    create shared/hooks/useTheme.js
    create shared/components/ThemeToggle.jsx
3. seperate navbar added
4. now for  dark theme correctly apply need to add dark class on each pages
    update ItemCard.jsx
    update ItemList.jsx
    update FilterBar.jsx
    update ItemForm.jsx
    update Modal.jsx
    update Loader.jsx

5. add cursor-pointer for any button or icon
6. add email and phone number validation (for correctness)
7. calender validation (no future date allowed)